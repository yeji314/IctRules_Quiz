# ================================================
# ICT Rules Quiz - 환경 설정
# ================================================
#
# 사용법:
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. 환경에 맞게 아래 값들을 수정
# 3. 운영 배포 시 JWT_SECRET, SESSION_SECRET 반드시 변경!
#
# ================================================

# ================================================
# 기본 서버 설정
# ================================================
PORT=5000
NODE_ENV=development
# production 배포 시: NODE_ENV=production

# ================================================
# 데이터베이스 (SQLite)
# ================================================
# SQLite는 파일 기반이므로 별도 설정 불필요
# 로컬: server/database/quiz.db
# Docker: /app/database/quiz.db (볼륨 마운트)

# ================================================
# JWT 설정
# ================================================
# ⚠️ 운영 환경에서는 반드시 안전한 랜덤 문자열로 변경!
# 생성 방법: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here_change_in_production
JWT_EXPIRES_IN=7d

# ================================================
# 세션 설정
# ================================================
# ⚠️ 운영 환경에서는 반드시 안전한 랜덤 문자열로 변경!
# 생성 방법: openssl rand -base64 32
SESSION_SECRET=your_session_secret_key_here_change_in_production

# ================================================
# CORS 설정 (프론트엔드 분리 시)
# ================================================
# 현재는 서버와 클라이언트가 같은 포트(5000)에서 서빙되므로 CORS 설정 불필요
# 프론트엔드를 분리할 경우 아래 환경에 맞게 설정:
#
# 로컬 개발 (프론트엔드를 3000 포트에서 개발):
CORS_ORIGIN=http://localhost:3000
#
# 개발 서버 배포:
# CORS_ORIGIN=https://ictrulesdev.com
#
# 운영 서버 배포:
# CORS_ORIGIN=https://ictrulesquiz.shinhan.com

# ================================================
# Swing SSO 설정
# ================================================
# SSO 사용 여부
SWING_SSO_ENABLED=false
# 운영 환경에서 SSO 사용 시: SWING_SSO_ENABLED=true

# SSO 환경 (mock / development / production)
SWING_SSO_ENV=mock
# 운영 환경: SWING_SSO_ENV=production

# SSO API 기본 URL
SWING_API_BASE_URL=http://127.0.0.1:8055/swing-mock-server
# 운영 환경: SWING_API_BASE_URL=https://swing-api.shinhan.com

# SSO API 엔드포인트
SWING_OAUTH_ENDPOINT=/cau/v1/oauth-code-simple
SWING_IDPW_ENDPOINT=/cau/v1/idpw-authorize

# SSO 클라이언트 인증 정보
SWING_CLIENT_ID=mock_client_id
SWING_CLIENT_SECRET=mock_client_secret
# ⚠️ 운영 환경에서는 실제 인증 정보로 변경!

# SSO 사용자 자동 생성
SWING_AUTO_CREATE_USER=true
SWING_SYNC_USER_INFO=true

# SSO 비밀번호 솔트
SWING_PASSWORD_SALT=your_password_salt_change_in_production
# ⚠️ 운영 환경에서는 반드시 변경!
# 생성 방법: openssl rand -base64 16

# SSO 접근 제어
SWING_ACCESS_CONTROL_TYPE=none
# Options: none, employee_list, department_list, collection
# SWING_EMPLOYEE_LIST_FILE=./config/employees.json
# SWING_DEPARTMENT_LIST_FILE=./config/departments.json
# SWING_ACCESS_COLLECTION_NAME=allowed_users

# ================================================
# 메신저 API (추후 추가)
# ================================================
# MESSENGER_API_URL=
# MESSENGER_API_KEY=

# ================================================
# 빠른 환경 설정 가이드
# ================================================
#
# 🏠 로컬 개발:
#   NODE_ENV=development
#   CORS_ORIGIN=http://localhost:3000
#   SWING_SSO_ENABLED=false
#
# 🔧 개발 서버:
#   NODE_ENV=development
#   CORS_ORIGIN=https://ictrulesdev.com
#   SWING_SSO_ENABLED=false (또는 true)
#
# 🚀 운영 서버:
#   NODE_ENV=production
#   CORS_ORIGIN=https://ictrulesquiz.shinhan.com
#   SWING_SSO_ENABLED=true
#   JWT_SECRET=<안전한_랜덤_문자열>
#   SESSION_SECRET=<안전한_랜덤_문자열>
#   SWING_CLIENT_ID=<실제_클라이언트_ID>
#   SWING_CLIENT_SECRET=<실제_클라이언트_SECRET>
#   SWING_PASSWORD_SALT=<안전한_솔트>
#
# ================================================
