# 관리자 페이지 가이드

## 📋 목차
1. [관리자 페이지 접근](#관리자-페이지-접근)
2. [대시보드 (통계)](#대시보드-통계)
3. [퀴즈 관리](#퀴즈-관리)
4. [주요 기능](#주요-기능)

---

## 관리자 페이지 접근

### 로그인
1. http://localhost:5000/pages/index.html 접속
2. 관리자 계정으로 로그인:
   - **행원번호**: `admin`
   - **비밀번호**: `admin@`
3. 자동으로 관리자 대시보드(`/pages/admin/dashboard.html`)로 이동

### 권한
- **관리자(`role: 'admin'`)만** 접근 가능
- 일반 사용자가 접근 시 자동으로 quiz-list.html로 리다이렉트

---

## 대시보드 (통계)

### 화면 구성
- **좌측 사이드바**: 메뉴 (홈, 퀴즈 관리)
- **우측 상단**: 관리자명 + 로그아웃 버튼
- **메인 영역**: 통계 정보

### 부서별 참여율

#### 기능
- 부서별 퀴즈 참여율을 게이지 차트로 시각화
- 이벤트별 필터링 가능

#### 게이지 색상
- **초록색 (70% 이상)**: 높은 참여율
- **노란색 (30-70%)**: 중간 참여율
- **빨간색 (30% 미만)**: 낮은 참여율

#### 표시 정보
- 부서명
- 참여율 (백분율)
- 참여 인원 / 전체 인원

### Lucky Draw 당첨 현황

#### 표시 정보
| 이름 | 부서 | 당첨일 | 상품 | 수령 여부 |
|------|------|--------|------|-----------|
| 홍길동 | IT부 | 2025-01-05 | 기프티콘 | 수령 완료 |
| 김철수 | 영업부 | 2025-01-06 | 기프티콘 | 대기 중 |

#### 수령 상태
- **수령 완료**: 초록색 배지
- **대기 중**: 노란색 배지

---

## 퀴즈 관리

### 이벤트 관리

#### 이벤트 목록
- 모든 퀴즈 이벤트를 카드 형태로 표시
- 각 이벤트당 표시 정보:
  - 이벤트명 (예: "2025년 1월")
  - 년월 (YYYY-MM)
  - 시작일 ~ 종료일
  - 문제 수 (일반 / Lucky Draw)
  - 참여자 수
  - 활성화 상태

#### 이벤트 생성
1. **"+ 이벤트 생성"** 버튼 클릭
2. 모달 창에서 정보 입력:
   - 이벤트명
   - 년월 (YYYY-MM 형식)
   - 시작일
   - 종료일
   - 활성화 여부
3. **"저장"** 버튼 클릭

#### 이벤트 수정
1. 해당 이벤트 카드에서 **"수정"** 버튼 클릭
2. 모달 창에서 정보 수정
3. **"저장"** 버튼 클릭

#### 이벤트 삭제
1. 해당 이벤트 카드에서 **"삭제"** 버튼 클릭
2. 확인 메시지:
   > 이벤트를 삭제하시겠습니까?
   > 연결된 모든 문제도 함께 삭제됩니다.
3. **"확인"** 클릭 시 삭제

**⚠️ 주의**: 이벤트 삭제 시 해당 이벤트의 모든 문제, 세션, 답변이 함께 삭제됩니다!

### 문제 관리

#### 문제 목록 보기
1. 이벤트 카드에서 **"문제 관리"** 버튼 클릭
2. 선택된 이벤트의 문제 목록이 하단에 표시됨

#### 문제 카드 정보
- 문제 유형 (드래그앤드롭, 타이핑, 빈칸맞추기, OX퀴즈, 틀린부분찾기)
- 카테고리 (일반 / Lucky Draw)
- 문제 내용
- 수정 / 삭제 버튼

#### 문제 추가
1. **"+ 문제 추가"** 버튼 클릭
2. 모달 창에서 정보 입력:
   - **문제 유형**: 드래그앤드롭, 타이핑, 빈칸맞추기, OX퀴즈, 틀린부분찾기
   - **카테고리**: 일반 / Lucky Draw
   - **문제 내용**: 문제 텍스트
   - **문제 데이터 (JSON)**: 문제별 데이터 구조
   - **해설**: 정답 해설 (선택)
3. **"저장"** 버튼 클릭

#### 문제 데이터 JSON 형식

##### 1. OX 퀴즈
```json
{
  "correct_answer": "O",
  "options": ["O", "X"]
}
```

##### 2. 빈칸 맞추기
```json
{
  "correct_answer": "3일",
  "options": ["1일", "3일", "5일", "7일", "10일"]
}
```

##### 3. 타이핑
```json
{
  "target": "정확히 따라 입력해야 하는 문장",
  "case_sensitive": false
}
```

##### 4. 드래그 앤 드롭
```json
{
  "items": ["항목1", "항목2", "항목3"],
  "targets": ["대상1", "대상2", "대상3"],
  "correct_mapping": {
    "항목1": "대상1",
    "항목2": "대상2",
    "항목3": "대상3"
  }
}
```

##### 5. 틀린 부분 찾기
```json
{
  "correct_answer": "항목3",
  "options": ["항목1", "항목2", "항목3", "항목4", "항목5"]
}
```

#### 문제 수정
1. 해당 문제 카드에서 **"수정"** 버튼 클릭
2. 모달 창에서 정보 수정
3. **"저장"** 버튼 클릭

#### 문제 삭제
1. 해당 문제 카드에서 **"삭제"** 버튼 클릭
2. 확인 메시지: "문제를 삭제하시겠습니까?"
3. **"확인"** 클릭 시 삭제

### 엑셀 대량 업로드

#### 기능
- Excel 파일(.xlsx)로 문제를 대량 등록 가능
- 한 번에 여러 문제 추가 가능

#### 사용 방법
1. **"📄 엑셀 업로드"** 버튼 클릭
2. Excel 파일 선택
3. **"업로드"** 버튼 클릭

#### Excel 파일 형식

| question_type | category | question_text | question_data (JSON) | explanation |
|---------------|----------|---------------|----------------------|-------------|
| ox | normal | 문제 내용 | {"correct_answer":"O","options":["O","X"]} | 해설 내용 |
| multiple | luckydraw | 문제 내용 | {"correct_answer":"3일","options":["1일","3일","5일"]} | 해설 내용 |

##### 열(Column) 설명
- **question_type**: 문제 유형 (`dragdrop`, `typing`, `multiple`, `ox`, `find_wrong`)
- **category**: 카테고리 (`normal`, `luckydraw`)
- **question_text**: 문제 내용
- **question_data**: 문제 데이터 (JSON 형식 텍스트)
- **explanation**: 해설 (선택)

**📝 참고**: 엑셀 업로드 기능은 `xlsx` 라이브러리 설치 후 사용 가능합니다.

---

## 주요 기능

### API 엔드포인트

#### 이벤트 관리
- `GET /api/admin/events` - 이벤트 목록 조회
- `POST /api/admin/events` - 이벤트 생성
- `PUT /api/admin/events/:id` - 이벤트 수정
- `DELETE /api/admin/events/:id` - 이벤트 삭제

#### 문제 관리
- `GET /api/admin/questions?event_id={id}` - 문제 목록 조회
- `POST /api/admin/questions` - 문제 생성
- `PUT /api/admin/questions/:id` - 문제 수정
- `DELETE /api/admin/questions/:id` - 문제 삭제

#### 통계
- `GET /api/admin/stats/overview` - 전체 통계
- `GET /api/admin/stats/departments?event_id={id}` - 부서별 통계
- `GET /api/admin/stats/questions?event_id={id}` - 문제별 통계
- `GET /api/admin/stats/users?event_id={id}` - 사용자 목록

#### Lucky Draw
- `POST /api/admin/luckydraw/draw` - 추첨
- `GET /api/admin/luckydraw/winners?event_id={id}` - 당첨자 목록
- `PUT /api/admin/luckydraw/:id/claim` - 수령 처리

### 보안

#### 인증
- JWT 토큰 기반 인증
- `Authorization: Bearer <token>` 헤더 필수

#### 권한
- 모든 관리자 API는 `authenticate` + `requireAdmin` 미들웨어로 보호
- `role: 'admin'`인 사용자만 접근 가능

### 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│                                      관리자님   [로그아웃] │
├──────────┬───────────────────────────────────────────────┤
│          │  📊 통계 대시보드                               │
│ ICT RULES│  ┌────────────────────────────────────────┐  │
│ 관리자 페이지│  │ 이벤트 선택: [전체 ▼]                  │  │
│          │  └────────────────────────────────────────┘  │
│ 📍 홈 (통계) │  ┌────────────────────────────────────────┐  │
│ 📝 퀴즈 관리 │  │ 부서별 참여율                           │  │
│          │  │ IT부:  ████████████░░░ 85% (17/20)     │  │
│          │  │ 영업부: ██████░░░░░░░ 50% (10/20)     │  │
│          │  └────────────────────────────────────────┘  │
│          │  ┌────────────────────────────────────────┐  │
│          │  │ Lucky Draw 당첨 현황                     │  │
│          │  │ 이름  │ 부서 │ 당첨일     │ 수령     │  │
│          │  │ 홍길동│ IT부 │ 2025-01-05 │ 수령완료 │  │
│          │  └────────────────────────────────────────┘  │
└──────────┴───────────────────────────────────────────────┘
```

---

## 문제 해결

### 403 Forbidden 에러
- 관리자 권한이 없는 계정으로 접근한 경우
- 해결: `role: 'admin'` 계정으로 로그인

### 401 Unauthorized 에러
- JWT 토큰이 없거나 만료된 경우
- 해결: 다시 로그인

### 이벤트/문제가 표시되지 않음
- 데이터베이스에 데이터가 없는 경우
- 해결: `npm run db:reset` 실행 (개발 환경)

### 엑셀 업로드 실패
- `xlsx` 라이브러리가 설치되지 않은 경우
- 해결: `npm install xlsx` 실행 후 백엔드 API 구현

---

## 추가 개발 필요 사항

### 엑셀 업로드 백엔드 구현
```javascript
// server/controllers/adminController.js
const XLSX = require('xlsx');
const multer = require('multer');

const upload = multer({ dest: 'uploads/' });

router.post('/questions/upload', upload.single('file'), async (req, res) => {
  try {
    const workbook = XLSX.readFile(req.file.path);
    const sheet = workbook.Sheets[workbook.SheetNames[0]];
    const data = XLSX.utils.sheet_to_json(sheet);

    // 문제 대량 생성
    const questions = await Promise.all(data.map(row => {
      return db.Question.create({
        event_id: req.body.event_id,
        question_type: row.question_type,
        category: row.category,
        question_text: row.question_text,
        question_data: JSON.parse(row.question_data),
        explanation: row.explanation
      });
    }));

    res.json({ success: true, count: questions.length });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
```

---

**작성일**: 2025-01-06  
**버전**: 1.0.0  
**작성자**: ICT 내규 퀴즈 개발팀

